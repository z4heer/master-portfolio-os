Good idea. Before we start reviewing and refactoring, it's worth doing a quick **integration smoke test** of the infrastructure. That way, if something fails later, we'll know whether it was introduced by the Auth refactoring or was already present.

From your `git status`, I can see you've made significant infrastructure changes:

* ✅ New interceptors (Auth, Error, Loading, Retry)
* ✅ New core services
* ✅ New state management
* ✅ New guards package
* ✅ Shared module/components
* ✅ Environment configuration
* ✅ Routing updates
* ✅ Product feature scaffold
* ✅ Authentication changes

So let's validate the **Core Infrastructure** first.

---

# Sprint 3.1A – Module 1 Integration Testing

## Preconditions

Verify:

* Backend running (`localhost:8000`)
* Angular app running

```bash
ng serve
```

Open

```
http://localhost:4200
```

There should be **no console errors** before login.

---

# Test 1 — Application Startup

### Expected

* Angular compiles successfully
* No build errors
* Home/Login page loads
* Browser console clean

### Verify

Developer Tools →

Console

Expected

```
No red errors
```

---

# Test 2 — Environment Configuration

Open

```
Network tab
```

Attempt Login.

Verify API URL

Expected

```
http://localhost:8000/api/v1/...
```

Not

```
undefined
```

or

```
localhost:4200/api
```

---

# Test 3 — Loading Spinner

Login with any credentials.

Expected

```
Click Login

↓

Spinner appears

↓

API finishes

↓

Spinner disappears
```

Verify:

* no flickering
* no spinner stuck forever

---

# Test 4 — Error Interceptor

Stop backend.

```bash
docker compose stop backend
```

Attempt Login.

Expected

* Spinner stops
* Friendly error message
* No endless loading
* No blank page
* No crash

Console should **not** show uncaught exceptions.

---

# Test 5 — Retry Interceptor

Keep backend stopped.

Login.

Expected

If retry interceptor is enabled:

```
Request

↓

Retry

↓

Retry

↓

Final Error
```

Verify:

* finite retries
* no infinite retry loop

---

# Test 6 — Auth Interceptor

Restart backend.

Login.

After login

Open

Developer Tools

↓

Network

↓

Protected API

Inspect Request Headers

Expected

```
Authorization:

Bearer eyJ...
```

---

# Test 7 — Route Configuration

Open

```
/login
```

Expected

Login page

Then

```
/
```

Expected

Redirect behaves as designed (login/dashboard depending on auth state).

---

# Test 8 — Guards

Without logging in

Navigate directly to

```
/dashboard
```

Expected

Redirect to Login.

After login

Navigate again.

Expected

Dashboard opens.

---

# Test 9 — Browser Refresh

Login.

Refresh browser (F5).

Expected

One of these (depending on your implementation):

* session remains active
* or redirects cleanly to login

Not acceptable:

* white page
* routing error
* undefined object
* blank component

---

# Test 10 — Logout

If logout is available

Verify

* token removed
* redirected to login
* protected pages inaccessible

---

# Test 11 — Network Requests

Network Tab

Expected

```
No duplicate login request

Exactly one POST /login
```

---

# Test 12 — Memory Leak

Login

Logout

Login

Logout

Repeat

Expected

* Spinner behaves correctly
* No duplicate HTTP requests
* No increasing number of subscriptions
* Stable memory usage

---

# Test 13 — Console Quality

Console should **not** contain:

❌

```
ExpressionChangedAfterItHasBeenChecked
```

❌

```
NullInjectorError
```

❌

```
Cannot match any routes
```

❌

```
NG04002
```

❌

```
InjectorError
```

❌

```
Unhandled Promise rejection
```

---

# Test 14 — Build Verification

Run

```bash
ng build
```

Expected

```
✔ Browser application bundle generation complete.
```

No compilation errors.

---

# Test 15 — Lint (if configured)

```bash
ng lint
```

Expected

```
All files pass.
```

---

# Test 16 — Authentication Persistence

Open DevTools →

Application →

Local Storage

Verify

```
access_token

refresh_token
```

After logout

Expected

```
Both removed
```

---

# Test Summary Template

Once you've completed the checks, you can summarize the results like this:

| Test              | Status          | Notes |
| ----------------- | --------------- | ----- |
| App Startup       | ⬜ Pass / ⬜ Fail |       |
| Environment       | ⬜ Pass / ⬜ Fail |       |
| Loading Spinner   | ⬜ Pass / ⬜ Fail |       |
| Error Interceptor | ⬜ Pass / ⬜ Fail |       |
| Retry Interceptor | ⬜ Pass / ⬜ Fail |       |
| Auth Interceptor  | ⬜ Pass / ⬜ Fail |       |
| Guards            | ⬜ Pass / ⬜ Fail |       |
| Routing           | ⬜ Pass / ⬜ Fail |       |
| Browser Refresh   | ⬜ Pass / ⬜ Fail |       |
| Logout            | ⬜ Pass / ⬜ Fail |       |
| Build             | ⬜ Pass / ⬜ Fail |       |
| Lint              | ⬜ Pass / ⬜ Fail |       |

### My observations from your current branch

A few things stand out from the `git status` output that we'll pay special attention to during the review:

* `admin.guard.ts` and `auth.guard.ts` were deleted, while a new `core/guards/` folder was added. We'll verify that routing references have been updated correctly and there are no stale imports.
* The retry interceptor filename appears as `retry.inrterceptor.ts`. If that's not intentional, it's worth renaming it to `retry.interceptor.ts` before committing to avoid confusion and maintain consistency.
* You're using the new Angular environment naming (`environment.development.ts`), which is the recommended approach. We'll confirm `angular.json` file replacements are configured correctly.
* There are many new untracked core folders (`services`, `state`, `shared`, `products`), so once testing passes, we'll review them together for architecture and production readiness.

After you complete this smoke test, send me:

1. The pass/fail summary.
2. Any console or network errors.
3. The contents of `auth.service.ts` and `login.component.ts`.

We'll then perform a detailed architecture review and production-grade refactoring for Sprint 3.1A.
