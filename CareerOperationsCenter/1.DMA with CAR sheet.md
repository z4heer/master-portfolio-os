Here is a review of your **Top 250 Stocks** sheet, the formulas used for these calculations, and suggestions to significantly improve the spreadsheet's performance.

### 1\. Actual Formulas for the "Top 250 Stocks" Sheet

Based on standard technical analysis and the data in your sheet, here are the formulas that should populate Row 2. You can drag these down for the rest of the list.

  * **CMP (Current Market Price) in `B2`**:
    ``` excel
    =GOOGLEFINANCE(A2, "price")
    
    ```
  * **50 DMA in `C2`**:
    *(Note: We use `TODAY()-80` to ensure we capture 50 actual trading days, as Google Finance skips weekends and holidays)*
    ``` excel
    =AVERAGE(QUERY(GOOGLEFINANCE(A2, "price", TODAY()-80, TODAY()), "SELECT Col2", 1))
    
    ```
  * **100 DMA in `D2`**:
    ``` excel
    =AVERAGE(QUERY(GOOGLEFINANCE(A2, "price", TODAY()-150, TODAY()), "SELECT Col2", 1))
    
    ```
  * **200 DMA in `E2`**:
    ``` excel
    =AVERAGE(QUERY(GOOGLEFINANCE(A2, "price", TODAY()-300, TODAY()), "SELECT Col2", 1))
    
    ```
  * **Output in `F2`**:
    *(This evaluates the trend based on moving average alignment)*
    ``` excel
    =IF(AND(B2>C2, C2>D2, D2>E2), "In Bull Run", IF(AND(B2<C2, C2<D2, D2<E2), "In Bear Run", "Unconfirmed"))
    
    ```
  * **Difference from 200 DMA in `G2`**:
    *(Calculated as a percentage difference to match your current outputs)*
    ``` excel
    =(B2-E2)/E2 * 100
    
    ```
  * **Cumulative Average Rule (CAR) Rating in `H2`**:
    *(This uses custom logic based on the 200 DMA difference. Here is a basic version matching your outputs)*
    ``` excel
    =IF(ISNA(E2), "Short History", IF(G2 > 0, "Buy/Average Out", "Avoid/Hold"))
    
    ```

-----

### 2\. Formula Performance Fixes & Suggestions

**The Problem:** Your sheet calculates 3 different DMAs for 250 stocks. This results in **750 separate historical `GOOGLEFINANCE` queries** running simultaneously every time you open the sheet. Google Sheets struggles with this volume, which is exactly why you are seeing `#N/A` errors (like in row 101 for `GUJGASLTD`) and experiencing heavy lagging.

Here are the best ways to fix the performance:

**Fix A: Consolidate Historical Data (Reduces API calls by 66%)**
Instead of fetching historical data 3 separate times (for 50, 100, and 200 days) for every single stock, fetch 300 days of data **once** per stock, and calculate all three averages from that single data pull.

  * **How:** You would create a hidden "Data" tab that pulls a single 300-day array for each stock. Then, your C, D, and E columns simply use the `AVERAGE` function on the last 50, 100, and 200 rows of that pulled data.

**Fix B: Use Google Apps Script (Best for 250+ stocks)**
`GOOGLEFINANCE` is not designed to handle hundreds of heavy historical array queries in real-time.

  * **How:** We can write a simple Google Apps Script that runs automatically once a day (e.g., after the market closes). The script fetches the 50, 100, and 200 DMAs in the background and pastes them as **static values** into your sheet. This keeps the sheet lightning-fast and completely eliminates loading errors.

**Fix C: Check for Valid Tickers**
For row 101 (`NSE:GUJGASLTD`), the `#N/A` error is occurring because the ticker symbol might be slightly off or temporarily unavailable in Google Finance. Always ensure the ticker matches exactly what Google Finance expects (e.g., `NSE:GUJGAS`).

Let me know if you would like me to provide the Google Apps Script for **Fix B**, or if you'd prefer to set up the consolidated data method in **Fix A**\!
